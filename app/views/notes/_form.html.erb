<%= form_with(model: note) do |form| %>
  <% if note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(note.errors.count, "error") %> prohibited this note from being saved:</h2>

      <ul>
        <% note.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title, class: 'form-field' %>
    <%= form.text_field :title, maxlength: 30, size: 30 %>
  </div>

  <div class="field">
    <%= form.label :body, class: 'form-field' %>
    <%= form.text_field :body, maxlength: 1000, size:30 %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
